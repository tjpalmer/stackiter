package stackiter.sim;

/**
 * Possible forms of activity for the tool.
 * The presumption was that other modes might exist, and I had CLEAR and FLUSH
 * here for a while but unused, preferring instead to use interaction with items
 * on the screen.
 * <p>
 * TODO Replace this with a boolean?
 */
public enum ToolMode {

	GRASP,

	INACTIVE

}
